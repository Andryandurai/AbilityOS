# AbilityOS's documented dev setup runs the backend on port 4343 (see
# README.md's Quick Start) — this is already the default frontend/.env.local
# uses. Copy this file to frontend/.env.local only if your backend runs on a
# different port/host.
VITE_API_BASE_URL=http://localhost:4343/api
